window.SIDEBAR_ITEMS = {"constant":[["CHAIN_CONFIG","The config for this chain"],["DAO_CONFIG","The config for communicating with the DAO"],["IBC_TRANSFER_MODULES","We store approved IBC transfer module connections: local connection_id: the light client of the remote chain we don’t track caller port id as it is bounded to the ibctransfer module on the remote chain"],["JOB_ID","Job Id for current dispatch, used for listening for callbacks for users. Will loop around if max is hit"]]};